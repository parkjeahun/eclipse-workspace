package q;

import p.A;

public class B extends A{
	void set() {
		pub = 1; //public : 모두 다른 패키지에서 접근
		pro = 2; //protacted : 같은 패키지일때 접근, 상속관계의 다른 패키지에서 접근
		def = 3; //default : 같은 패키지 일때 접근, 다른 패키지에서는 접근 불가
		pri = 4; //private : 다름 클래스에서 접근 차단
	}
}
